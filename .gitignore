# --- WAILS / GO ---
# Binaries and build artifacts
build/bin/
build/windows/
build/darwin/
build/linux/
*.exe
*.dll
*.so
*.dylib

# Wails generated bindings (Optional: Keep this commented if you want to commit bindings for CI)
# frontend/wailsjs/

# Go dependencies (if you use vendor)
vendor/

# --- FRONTEND (Next.js) ---
frontend/node_modules/
frontend/.next/
frontend/out/
frontend/build/
frontend/dist/
frontend/.vercel/

# Environment variables
.env
.env.local
.env.*.local
frontend/.env
frontend/.env.local

# npm / yarn / pnpm debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# --- SYSTEM / IDE ---
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo

# --- APP SPECIFIC ---
# Ignore local user data if you accidentally save it in the repo
# (Your app saves to Documents/MotionStudio, so this is just a safeguard)
*.json

# --- WHITELIST (Files we actually WANT to commit) ---
!package.json
!package-lock.json
!tsconfig.json
!wails.json
!frontend/package.json
!frontend/tsconfig.json